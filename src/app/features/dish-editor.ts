import { Component, inject, signal } from '@angular/core';
import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';
import { ActivatedRoute, Router, RouterLink } from '@angular/router';
import { RestaurantStore, toast } from '../core/store';
import { FALLBACK_IMAGE } from '../core/mock-data';

@Component({selector:'app-dish-editor',imports:[ReactiveFormsModule,RouterLink],template:`<div class="page"><div class="page-heading"><div><a routerLink="/catalogo" class="back-link"><span class="material-symbols-rounded">arrow_back</span> Volver al catálogo</a><h1>{{id?'Editar plato':'Nuevo plato'}}</h1><p>{{id?'Actualiza los detalles de tu plato.':'Agrega una nueva creación a tu menú.'}}</p></div></div><form [formGroup]="form" (ngSubmit)="save()" class="editor-grid"><section class="surface form-card"><div class="section-heading"><div><h2>Información del plato</h2><p>Los detalles que verán en el catálogo</p></div></div><div class="form-grid"><div class="field full"><label for="dish-name">Nombre del plato <span class="required">*</span></label><input id="dish-name" class="control" formControlName="name" placeholder="Ej. Lomo Saltado">@if(invalid('name')){<small class="field-error">Ingresa un nombre de al menos 3 caracteres.</small>}</div><div class="field full"><label for="dish-description">Descripción <span class="required">*</span></label><textarea id="dish-description" class="control" rows="4" formControlName="description" placeholder="Describe ingredientes y preparación"></textarea>@if(invalid('description')){<small class="field-error">Describe el plato con al menos 10 caracteres.</small>}</div><div class="field"><label for="dish-category">Categoría <span class="required">*</span></label><select id="dish-category" class="control" formControlName="categoryId"><option [ngValue]="0">Seleccionar categoría</option>@for(cat of store.categories();track cat.id){<option [ngValue]="cat.id">{{cat.name}}</option>}</select>@if(invalid('categoryId')){<small class="field-error">Selecciona una categoría.</small>}</div><div class="field"><label for="dish-price">Precio (S/) <span class="required">*</span></label><input id="dish-price" class="control" type="number" min="0.1" step="0.01" formControlName="price" placeholder="0.00">@if(invalid('price')){<small class="field-error">Ingresa un precio mayor a cero.</small>}</div><div class="field full"><label for="dish-image">URL de la imagen <span class="required">*</span></label><input id="dish-image" class="control" formControlName="image" placeholder="https://..."><small class="field-help">Usa una imagen de buena resolución. Se mostrará una vista previa.</small>@if(invalid('image')){<small class="field-error">Ingresa una URL de imagen válida.</small>}</div><div class="field full"><label class="switch-row"><input type="checkbox" formControlName="available"><span class="switch"></span><span><strong>Plato disponible</strong><small>Los clientes podrán incluirlo en nuevos pedidos.</small></span></label></div></div><div class="form-actions"><a routerLink="/catalogo" class="btn btn-secondary">Cancelar</a><button class="btn btn-primary" type="submit"><span class="material-symbols-rounded">save</span>{{id?'Guardar cambios':'Crear plato'}}</button></div></section><aside class="surface preview-card"><div class="section-heading"><div><h2>Vista previa</h2><p>Así se verá en el catálogo</p></div></div><div class="preview-dish"><img [src]="form.controls.image.value||fallbackImage" alt="Vista previa del plato" (error)="fallback($event)"><div><span class="dish-category">{{categoryName()}}</span><h3>{{form.controls.name.value||'Nombre del plato'}}</h3><p>{{form.controls.description.value||'La descripción del plato aparecerá aquí.'}}</p><strong>S/ {{(+form.controls.price.value||0).toFixed(2)}}</strong></div></div></aside></form></div>`})
export class DishEditorPage {
  readonly store=inject(RestaurantStore);private route=inject(ActivatedRoute);private router=inject(Router);private fb=inject(FormBuilder);readonly id=Number(this.route.snapshot.paramMap.get('id'))||0;readonly fallbackImage=FALLBACK_IMAGE;
  readonly form=this.fb.nonNullable.group({name:['',[Validators.required,Validators.minLength(3)]],description:['',[Validators.required,Validators.minLength(10)]],categoryId:[0,Validators.min(1)],price:[0,Validators.min(.1)],image:['',[Validators.required,Validators.pattern(/^https?:\/\/.+/)]],available:[true]});
  constructor(){const dish=this.store.dishes().find(d=>d.id===this.id);if(dish)this.form.patchValue(dish);}
  invalid(name:keyof typeof this.form.controls){const c=this.form.controls[name];return c.touched&&c.invalid;}
  categoryName(){return this.store.categories().find(c=>c.id===this.form.controls.categoryId.value)?.name??'Categoría';}
  fallback(event:Event){(event.target as HTMLImageElement).src=FALLBACK_IMAGE;}
  save(){this.form.markAllAsTouched();if(this.form.invalid)return;this.store.upsertDish(this.form.getRawValue(),this.id||undefined);toast(this.id?'Plato actualizado correctamente':'Plato creado correctamente');void this.router.navigateByUrl('/catalogo');}
}
